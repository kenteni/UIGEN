Write comprehensive tests for: $ARGUMENTS

Testing conventions:

- Use Vitests with React Testing Library
- Place test files in **tests** directory in the same folder as the source file
- Name test files a [filename].test.ts(x)
- Use @/ prefix for import

Coverage:

- Test happy paths
- Test edge cases
- Test error states
- Focus on testing behavior and public API's rather than implementation details.
